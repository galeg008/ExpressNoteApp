const router = require("express").Router();

router.get('/', (req,res)=> {
    //get all the notes from DB
})

router.post('/', (req,res)=> {
    //add a note to the DB
});



//bonus is to be able to delete
module.exports = router;